This is an HTML5 Joomla 1.5 template that has a nice mobile navigation utility built in. The template detects the user's location on the site, and if on a content page the menu is loaded in a mootools accordion.